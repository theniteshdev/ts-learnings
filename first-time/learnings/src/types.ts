// types in typescript
// annotation means -> needs to explaining
// interface means -> self understood

const username: string = "theniteshdev";
console.log(username);

var test = "Hmm";
console.log(test);

// test2 = "Hn Ji";
// this is not working because "use strict" mode enabled


// this is type annotation(batana-parta-hai)
const socialMedia:string = "twitter";
const userId:number = 33;
const hasPremium:boolean = false;

// this is inference(samajhdar)
const isAdmin = false;
const fruits = ['mangoes', 'apples', 'grapes'];